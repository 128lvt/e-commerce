/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['https://206.189.42.99:8443','localhost'], // Thêm 'localhost' vào đây
      },
};

export default nextConfig;
